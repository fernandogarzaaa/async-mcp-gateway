# async-mcp-gateway
A high-performance, asynchronous multi-tenant reverse proxy and load balancer for the Model Context Protocol (MCP). Built with FastAPI and Redis, featuring sub-10ms streaming stream-interception, non-blocking stdio process pooling, atomic token-bucket rate-limiting, and resilient provider failover.
